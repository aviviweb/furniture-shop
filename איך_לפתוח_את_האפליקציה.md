# 🌐 איך לפתוח את האפליקציה דרך הדפדפן

## 🔍 איך למצוא את ה-URL:

### דרך 1: Railway Dashboard (הכי קל)

1. **פתח Railway Dashboard** → **https://railway.app**
2. **לחץ על הפרויקט שלך**
3. **לחץ על `@furniture/web` Service**
4. **לחץ על "Settings"** (משמאל)
5. **גלול למטה** → **"Domains"** או **"Public URL"**
6. **תראה URL** כמו: `https://your-app-name.railway.app`
7. **לחץ עליו** או **העתק** → **פתח בדפדפן**

---

### דרך 2: דרך CLI

```powershell
# בדוק את ה-URL של Web Service
railway status
```

או:

```powershell
# פתח את ה-Dashboard בדפדפן
railway open
```

---

### דרך 3: בדוק ב-Logs

1. **Web Service** → **"Logs"**
2. **חפש הודעות כמו:**
   ```
   Ready on http://localhost:3000
   ```
   או:
   ```
   Server running on port 3000
   ```

**הערה:** זה ה-URL הפנימי, לא ה-URL הציבורי.

---

## 📋 צעדים מהירים:

1. **Railway Dashboard** → **`@furniture/web` Service**
2. **"Settings"** → **"Domains"** או **"Public URL"**
3. **לחץ על ה-URL** → **יפתח בדפדפן**

---

## 🆘 אם אין URL:

### אפשרויות:

1. **Railway יוצר URL אוטומטית** → בדוק ב-"Settings" → "Domains"
2. **אם אין** → לחץ **"+ Generate Domain"** או **"+ Add Domain"**
3. **Railway ייתן לך URL** כמו: `https://your-app-name.up.railway.app`

---

## ✅ אחרי שתמצא את ה-URL:

1. **פתח את ה-URL בדפדפן**
2. **אמור לראות את דף הבית** או **דף Login**
3. **התחבר עם:**
   - Email: `super@platform.local`
   - Password: `changeme`

---

## 🎯 סיכום:

**הדרך הכי קלה:**
1. Railway Dashboard → `@furniture/web` Service
2. Settings → Domains/Public URL
3. לחץ על ה-URL

**זה הכל!** 🚀

