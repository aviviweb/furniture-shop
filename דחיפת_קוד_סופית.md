# 🚀 דחיפת קוד סופית - תיקון apiDelete

## 🔴 הבעיה:

הפונקציה `apiDelete` קיימת בקוד המקומי, אבל Railway עדיין לא רואה אותה.

**הקוד לא נדחף ל-GitHub!**

---

## ✅ פתרון מיידי:

### שלב 1: ודא שהקוד שונה

**פתח `apps/web/lib/api.ts`** → **וודא שיש:**

```typescript
export async function apiDelete<T>(path: string, tenantId?: string): Promise<T> {
  // ... הקוד המלא
}
```

**אם זה לא קיים** → הוסף את זה בסוף הקובץ.

---

### שלב 2: דחוף את הקוד ל-GitHub

```powershell
git add apps/web/lib/api.ts
git commit -m "Fix: Add apiDelete export to api.ts"
git push
```

---

### שלב 3: בדוק ב-GitHub

1. **פתח GitHub** → ה-repo שלך
2. **עבור ל-`apps/web/lib/api.ts`**
3. **וודא שיש `export async function apiDelete`** (שורה 78)
4. **אם זה לא קיים** → הקוד לא נדחף, צריך לדחוף שוב

---

### שלב 4: Redeploy ב-Railway

1. **Dashboard** → **`@furniture/web`** → **Deployments**
2. **לחץ "Redeploy"** (או חכה ל-auto-deploy)
3. **בדוק את ה-Logs** → וודא שהבילד עובר

---

### שלב 5: נקה Build Cache (אם צריך)

**אם Redeploy לא עובד:**

1. **Dashboard** → **`@furniture/web`** → **Settings** → **Build**
2. **חפש "Clear Build Cache"** או **"Rebuild"**
3. **לחץ על זה**
4. **Redeploy**

---

## 💡 למה זה קורה:

- **הקוד המקומי שונה מהקוד ב-GitHub** → צריך לדחוף
- **Railway משתמש ב-Cache** → צריך Redeploy
- **הקוד לא נדחף** → `git push` לא עבד

---

## ✅ Checklist:

- [ ] `apiDelete` קיים ב-`apps/web/lib/api.ts`
- [ ] `git add apps/web/lib/api.ts`
- [ ] `git commit -m "..."`  
- [ ] `git push`
- [ ] בדוק ב-GitHub שהקוד שם
- [ ] Redeploy ב-Railway
- [ ] בדוק שה-Build עובר

---

**בואו נדחוף את הקוד עכשיו!**


