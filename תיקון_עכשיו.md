# 🔧 תיקון מיידי - מה שאתה רואה עכשיו

## ✅ מה שכבר נכון:
- ✅ Start Command: `pnpm --filter @furniture/api start` - **נכון!**
- ✅ API URL: `furnitureapi-production-ebea.up.railway.app` - **יש לך את זה!**

## ❌ מה שצריך לתקן:

### Build Command לא שלם!

**מה שאתה רואה עכשיו:**
```
pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma ge
```

**מה שצריך להיות (העתק את כל השורה הזו):**
```
pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma generate && pnpm --filter @furniture/api build
```

---

## 📝 איך לתקן (30 שניות):

1. **בשדה "Custom Build Command"** - מחק הכל (Ctrl+A, Delete)
2. **העתק את השורה הזו:**
   ```
   pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma generate && pnpm --filter @furniture/api build
   ```
3. **הדבק** (Ctrl+V)
4. **לחץ "Save"**

---

## 🎯 אחרי התיקון:

1. **לחץ "Deployments"** (משמאל)
2. **לחץ "Redeploy"**
3. **בחר `main`** → **"Deploy"**
4. **חכה 3-5 דקות**

---

## 📋 מה עוד צריך לעשות:

אחרי שה-API יעלה, תצטרך:

1. **לעשות אותו דבר ל-Web Service** - ראה `פשוט_וקצר.md` שלב 3
2. **להוסיף Environment Variables** - ראה `פשוט_וקצר.md` שלבים 2 ו-4
3. **להריץ Migrations** - ראה `פשוט_וקצר.md` שלב 6

---

**תתחיל עם התיקון הזה עכשיו!** 🚀

