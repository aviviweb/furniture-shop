# 🚀 העלאת האפליקציה - מדריך פשוט

## שלב 1: תיקון API Service (2 דקות)

1. **פתח Railway Dashboard** → בחר את הפרויקט
2. **לחץ על `@furniture/api`**
3. **לחץ על "Settings"** (משמאל)
4. **גלול למטה ל-"Build & Deploy"**

### Build Command:
**מחק הכל** והדבק:
```
pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma generate && pnpm --filter @furniture/api build
```

### Start Command:
**מחק הכל** והדבק:
```
pnpm --filter @furniture/api start
```

5. **לחץ "Save"**

---

## שלב 2: הגדרת משתנים ל-API (2 דקות)

1. **עדיין ב-API Service** → לחץ על **"Variables"** (משמאל)
2. **הוסף/תקן את המשתנים הבאים:**

לחץ **"+ New Variable"** לכל אחד:

```
DEMO_MODE = false
```

```
PORT = 4000
```

```
FRONTEND_URL = https://furnitureweb-production.up.railway.app
```
*(החלף ב-URL האמיתי של Web Service שלך)*

```
JWT_SECRET = <צור מפתח - ראה למטה>
```

**יצירת JWT_SECRET:**
פתח PowerShell והרץ:
```powershell
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
העתק את התוצאה והדבק ב-JWT_SECRET.

3. **שמור הכל**

---

## שלב 3: תיקון Web Service (2 דקות)

1. **חזור לרשימת Services** → לחץ על **`@furniture/web`**
2. **Settings** → **Build & Deploy**

### Build Command:
**מחק הכל** והדבק:
```
pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma generate && pnpm --filter @furniture/web build
```

### Start Command:
**מחק הכל** והדבק:
```
pnpm --filter @furniture/web start
```

3. **לחץ "Save"**

---

## שלב 4: הגדרת משתנים ל-Web (2 דקות)

1. **עדיין ב-Web Service** → לחץ על **"Variables"**
2. **הוסף/תקן:**

```
NEXT_PUBLIC_API_URL = https://furnitureapi-production-ebea.up.railway.app/api
```
*(זה ה-URL שלך - העתק אותו בדיוק כמו שכתוב כאן)*

```
NEXT_PUBLIC_TENANT_ID = furniture-demo
```

```
NEXT_PUBLIC_BRAND_NAME = Furniture Shop
```

```
NEXT_PUBLIC_PRIMARY_COLOR = #0ea5e9
```

```
NEXT_PUBLIC_DEMO_MODE = false
```

```
NODE_ENV = production
```

```
PORT = 3000
```

3. **שמור הכל**

---

## שלב 5: תיקון Worker Service (1 דקה)

1. **חזור לרשימת Services** → לחץ על **`@furniture/worker`**
2. **Settings** → **Build & Deploy**

### Build Command:
**מחק הכל** והדבק:
```
pnpm install --frozen-lockfile && pnpm --filter @furniture/prisma generate
```

### Start Command:
**מחק הכל** והדבק:
```
pnpm --filter @furniture/worker start
```

3. **לחץ "Save"**

---

## שלב 6: הרצת Migrations (1 דקה)

1. **חזור ל-API Service** (`@furniture/api`)
2. **לחץ על "Deployments"** (משמאל)
3. **לחץ על "Run Command"** או **"Shell"**
4. **הדבק את הפקודה:**
```
pnpm --filter @furniture/prisma migrate deploy
```
5. **לחץ Enter** וחכה לסיום

---

## שלב 7: פריסה מחדש (2 דקות)

1. **API Service** → **Deployments** → **"Redeploy"** → בחר `main` → **"Deploy"**
2. **Web Service** → **Deployments** → **"Redeploy"** → בחר `main` → **"Deploy"**
3. **Worker Service** → **Deployments** → **"Redeploy"** → בחר `main` → **"Deploy"**

**חכה 3-5 דקות** עד שהכל יעלה.

---

## ✅ בדיקה

1. **פתח את ה-URL של Web Service** - האפליקציה אמורה לעבוד!
2. **אם יש בעיה** → בדוק **Logs** בכל Service

---

## 🆘 איפה למצוא URLs?

**Railway Dashboard:**
- כל Service → **Settings** → **Networking** → שם תראה את ה-URL

או:
- כל Service → **Deployments** → לחץ על Deployment → שם תראה את ה-URL

---

**זה הכל! 🎉**

